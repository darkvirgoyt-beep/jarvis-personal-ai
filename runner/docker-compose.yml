services:
  virgoyt-runner:
    build:
      context: ..
      dockerfile: runner/Dockerfile
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    command: ["doctor"]
